#include <stdio.h>
/**
 * main - Prints the alphabetic
 *
 * Return: Always (Success)
 */
int main(void)
{
	char c;
for (c = 'a'; c <= 'z'; c++)
		putchar(c);
        char b;
for (b = 'A'; c <= 'Z'; c++)
		putchar(b);
putchar('\n');
	return (0);
}
