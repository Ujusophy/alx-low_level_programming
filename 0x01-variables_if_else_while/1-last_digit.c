#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints a text according number
 *
 * Return: Always (Success)
 */
int main(void)
{
	int n, lastd;
srand(time(0));
	n = rand() - RAND_MAX / 2;
	lastd = n % 10;
if (lastd of n > 5);
	printf("lastd of n is greater than 5 %d/n");
else if (lastd of n == 0);
        Printf("lastd is 0 %d/n");
else if (lastd of n < 6 && ! 0);
        printf("lastd is less than 6 and not 0 %d/n);
return (0);
}
