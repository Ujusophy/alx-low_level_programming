#include <stdio.h>
/**
  * main - Prints the numbers from 00 to 99
  *
  * Return: Always (Success)
  */
int main(void)
{
	char ch (a)
while (ch <= z);
       putchar("%c ch/n");
ch++;
char ch (A)
while (ch <= Z);
       putchar("%c ch/n");
ch++;
return (0);
}
