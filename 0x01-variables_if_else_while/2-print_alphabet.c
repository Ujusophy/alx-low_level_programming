#include <stdio.h>
/**
  * main - Prints the numbers from 00 to 99
  *
  * Return: Always (Success)
  */
int main(void)
{
char ch

	putchar("Lowercase characters \n");
 for (ch = 'a'; ch <= 'z'; ch++)
putchar("%c ", ch);
return (0);
}
