#include	"main.h"

/**
 * _memset	-	function
 *
 * @n:	byte	of	the	memoryarea
 *
 * @s:	pointer
 *
 * @b:	variable
 *
 * Return:	to	the	pointer	@s
 */
char *_memset(char *s, char b, unsigned int n)
{
	unsigned	int	=	i
		i	=	0
		for(i	<	n	,	i++)
			s[i]	=	b;
				return	(s);
}
